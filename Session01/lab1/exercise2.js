var calculatePoints = (_w, _d, _l) => {
    const win = _w *3;
    const drwas = _d * 1;
    const sum = win + drwas;
    console.log(sum);
}

calculatePoints(3, 4, 2);
calculatePoints(5, 0, 2);
calculatePoints(0, 0, 1);
